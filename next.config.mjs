/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Se você não estiver servindo nada da raiz, deixe simples.
  // O executive-panel continua usando o próprio next.config.mjs dentro de apps/executive-panel.
};

export default nextConfig;